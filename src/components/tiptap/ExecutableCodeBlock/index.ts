/**
 * ExecutableCodeBlock - Interactive TipTap code blocks
 * 
 * Exports the extension and view for use in TipTap editors.
 */

export { ExecutableCodeBlock, default as ExecutableCodeBlockExtension } from './ExecutableCodeBlockExtension';
export { default as ExecutableCodeBlockView } from './ExecutableCodeBlockView';
